{
  "name": "zen-design-monorepo",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Monorepo containing Zen Design System and applications",
  "workspaces": [
    "lib/*",
    "domain",
    "app"
  ],
  "scripts": {
    "dev": "yarn workspace app dev",
    "build": "yarn workspace @zen-design/ui build && yarn workspace app build",
    "build:ui": "yarn workspace @zen-design/ui build",
    "build:app": "yarn workspace app build",
    "storybook": "yarn workspace @zen-design/ui storybook",
    "build-storybook": "yarn workspace @zen-design/ui build-storybook",
    "test": "yarn workspace @zen-design/ui test:run && yarn workspace app test:run",
    "test:ui": "yarn workspace @zen-design/ui test",
    "test:app": "yarn workspace app test",
    "test:run": "yarn workspace @zen-design/ui test:run && yarn workspace app test:run"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "packageManager": "yarn@1.22.0",
  "license": "MIT"
}
